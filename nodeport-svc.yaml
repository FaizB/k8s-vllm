apiVersion: v1
kind: Service
metadata:
  name: vllm-nodeport
  labels:
    app: vllm-cpu-poc
spec:
  type: NodePort
  ports:
    - port: 8000
      targetPort: 8000
      nodePort: 30002
  selector:
     app: vllm-cpu-poc